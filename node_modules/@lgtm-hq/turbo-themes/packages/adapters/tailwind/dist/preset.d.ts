import { getTheme } from '../../core/dist/index.js';
import { themeIds } from '../../core/dist/index.js';
import { themesById } from '../../core/dist/index.js';

/**
 * Create color mappings for the given theme IDs
 * Generates classes like `bg-theme-{id}`, `text-theme-{id}-primary`, etc.
 */
export declare function createColorMappings(themeIds: readonly string[]): Record<string, ThemeColors>;

export { getTheme }

declare function preset(userOptions?: TurboThemePresetOptions): {
    theme: {
        extend: {
            colors: {
                background: string;
                surface: string;
                'surface-alt': string;
                foreground: string;
                'foreground-secondary': string;
                'foreground-inverse': string;
                muted: string;
                primary: string;
                brand: string;
                success: string;
                error: string;
                warning: string;
                info: string;
                danger: string;
                border: string;
                'border-strong': string;
                accent: string;
                link: string;
                heading: {
                    1: string;
                    2: string;
                    3: string;
                    4: string;
                    5: string;
                    6: string;
                };
                body: {
                    primary: string;
                    secondary: string;
                };
                selection: {
                    fg: string;
                    bg: string;
                };
                blockquote: {
                    border: string;
                    fg: string;
                    bg: string;
                };
                code: {
                    inline: {
                        fg: string;
                        bg: string;
                    };
                    block: {
                        fg: string;
                        bg: string;
                    };
                };
                table: {
                    border: string;
                    stripe: string;
                    thead: string;
                    cell: string;
                    header: string;
                };
            };
            spacing: {
                'turbo-xs': string;
                'turbo-sm': string;
                'turbo-md': string;
                'turbo-lg': string;
                'turbo-xl': string;
            };
            boxShadow: {
                'elevation-sm': string;
                'elevation-md': string;
                'elevation-lg': string;
                'elevation-xl': string;
            };
            transitionDuration: {
                fast: string;
                normal: string;
                slow: string;
            };
            transitionTimingFunction: {
                default: string;
                emphasized: string;
            };
            fontFamily: {
                sans: string;
                mono: string;
            };
        };
    };
};
export default preset;

/**
 * Color mappings for Tailwind preset
 *
 * Generates semantic color classes for each theme variant.
 */
declare type ThemeColors = {
    primary: string;
    surface: string;
    background: string;
    foreground: string;
    accent: string;
    success: string;
    warning: string;
    danger: string;
    info: string;
    border: string;
};

export { themeIds }

export { themesById }

/**
 * Tailwind CSS preset for Turbo Themes
 *
 * Maps Turbo Theme design tokens to Tailwind utility classes.
 * Supports all theme variants and provides semantic color mappings.
 *
 * @example
 * ```js
 * // tailwind.config.js
 * module.exports = {
 *   presets: [require('@lgtm-hq/turbo-themes/adapters/tailwind/preset')],
 *   theme: { extend: {} },
 *   plugins: [],
 * };
 * ```
 */
export declare interface TurboThemePresetOptions {
    /** Which themes to include in the preset */
    themes?: string[];
    /** Whether to include dark mode variants */
    darkMode?: boolean;
    /** Custom color mappings */
    colors?: Record<string, string>;
}

export { }
